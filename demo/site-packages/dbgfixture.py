# Import target for the debugger tests: gives them a frame in a module under
# res://site-packages/, which pocketpy names differently from a Godot script.
def add(a, b):
    result = a + b  # BREAK:inside_helper
    return result
